# server
spring.application.name=sp
server.port=80
server.servlet.context-path=/

# jsp
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# db(jsp, supabase postgres)
# 데이터베이스 연결 정보
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres
spring.datasource.username=postgres.sesibubsxstrroidchjs
spring.datasource.password=java1234
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA 설정  # none == CREATE, ALTER, DROP x
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# SQL 파라미터까지 로그로 보기(Hibernate 6 / Boot 3 기준)
logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.orm.jdbc.bind=trace
